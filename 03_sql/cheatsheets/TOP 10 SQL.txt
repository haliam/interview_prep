--TOP 10 SQL--

--01 
--IDENTIFICAR TABLA A PARTIR DEL NOMBRE DE UNA COLUMNA

	SELECT *
	FROM INFORMATION_SCHEMA.COLUMNS 
	WHERE COLUMN_NAME IN ('PRE_FK_PETICION_CAPACIDADES_UNIDADES_ID')

--02
--OBTENER LOS VALORES DE DISEÑO DE UNA COLUMNA

	SELECT *
	FROM INFORMATION_SCHEMA.COLUMNS 
	WHERE TABLE_NAME = 'RH_CONFIGURACION'
	AND COLUMN_NAME = 'RHP_FK_RH_CONEXION_ID'

--03
--OBTENER VALORES BÁSICOS DISEÑO DE UNA COLUMNA

	SELECT  COLUMN_NAME, DATA_TYPE,COLUMN_DEFAULT,IS_NULLABLE
	FROM INFORMATION_SCHEMA.COLUMNS 
	WHERE TABLE_NAME = 'RH_CONFIGURACION'
	AND COLUMN_NAME = 'RHP_FK_RH_CONEXION_ID'

--04
--OBTENER NÚMERO DE COLUMNAS DE UNA TABLA

	SELECT COUNT(*)
	FROM information_schema.columns
	WHERE table_name = 'RH_CONFIGURACION'

--05
-- CREAR TABLA

	CREATE TABLE Persons (
		PersonID int,
		LastName varchar(255),
		FirstName varchar(255),
		Address varchar(255),
		City varchar(255)
	);

--06
-- ACTUALIZAR TABLA CON FK

	ALTER TABLE [dbo].[RV_OBJETIVO_INTEGRACION]
	ADD RV_OI_FK_PERSONA_ID NUMERIC(18, 0)
	ADD FOREIGN KEY (RV_OI_FK_PERSONA_IDO) 
	REFERENCES [dbo].[PERSONA] (PER_ID)


--07
-- BORRAR COLUMNA DE UNA TABLA

	ALTER TABLE RV_OBJETIVO_INTEGRACION
	DROP COLUMN RV_OI_EMPLEADO_ID;



--08 CAMBIAR EL NOMBRE DE UNA COLUMNA


	EXEC sp_rename 'PETICION_PROCESO.PPR_HORA_CORTE_APLICA_DIA_SIGUIENTE', 'PPR_HORA_CORTE_APLICA_PARA_MAÑANA';



-- MOSTRAR EMPRESA ID Y NOMBRE
	SELECT EMP_ID, EMP_NOMBRE
	FROM LocalFlex.dbo.EMPRESA
	ORDER BY EMP_NOMBRE